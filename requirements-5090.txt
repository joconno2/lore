# Pinned deps for the local 5090 learner box.
# CUDA 13.0 required for sm_120 (Blackwell / RTX 5090).
#
#   uv pip install --index-url https://download.pytorch.org/whl/cu130 torch
#   uv pip install -r requirements-5090.txt
#
torch==2.12.0+cu130
numpy==1.26.4
ray[default]==2.46.0
nle==1.2.0
minihack==1.0.2
gymnasium==1.0.0
hydra-core==1.3.2
omegaconf==2.3.0
pytest==8.3.3
pytest-timeout==2.3.1
ruff==0.6.9
